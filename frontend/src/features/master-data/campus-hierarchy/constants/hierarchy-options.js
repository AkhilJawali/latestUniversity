// A4-410 — option lists for campus-hierarchy forms.
// degreeType is a free-text String on the backend (A4-2 ProgramDto), so these are
// convenience presets, not a locked enum; the field also accepts other values.
export const DEGREE_TYPES = [
  'B.TECH',
  'M.TECH',
  'B.SC',
  'M.SC',
  'MBA',
  'PHD',
];
