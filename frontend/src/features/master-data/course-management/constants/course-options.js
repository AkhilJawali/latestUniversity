// A4-415 — course type options. Fixed enum on the backend (A4-3 CreateCourseRequest
// regex ^(CORE|ELECTIVE|AUDIT)$), so this IS a locked list, not free text.
export const COURSE_TYPES = ['CORE', 'ELECTIVE', 'AUDIT'];
